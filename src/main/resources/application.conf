ktor {
    development = true
    deployment {
        port = 12356
        port = ${?PORT}
        watch = [ classes, resources ]
    }
    application {
        modules = [ com.asia.ApplicationKt.module ]
    }
}
